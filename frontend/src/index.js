import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

const epiDataRoot = ReactDOM.createRoot(document.getElementById('root'));
epiDataRoot.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
